# CLAIM-BACKEND-TENSTORRENT-GDN-DEVICE-PURE

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-BACKEND-TENSTORRENT-GDN-DEVICE-PURE` | `BACKEND-TENSTORRENT-GDN-DEVICE-PURE` (`ACTIVE`) | Maki (zai-glm-5.3-flash), helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/tenstorrent-gdn-device-pure.md` | isolated worktree `/tmp/row-tt-gdn-pure`; local Blackhole P150 under the `${GPU_LOCK:-$HOME/gpu.lock}` file mutex (not an `rc` fleet device) | `row/BACKEND-TENSTORRENT-GDN-DEVICE-PURE`, base `c64aa6d66` (the spec commit), issue [#2907](https://github.com/mudler/vllm.cpp/issues/2907) | Owns ONLY: the device-resident forms of the decode-side GDN ops (`CausalConv1dUpdateKernel`, `GdnDecodeKernel`, `GdnPostConvKernel`, `RmsNormGatedKernel`, `AttnQkNormRopeGateKernel`, device-side cos_sin production), the conv/ssm dual-role shadow-coherence fix (`tt_boundary` port, `ConvShadowServeable`, CHECK-before-download reorders, the kCastBf16/kCastF32 host-current fallback), the captured + eager pair refresh and its commit, the `Qwen3_5ForConditionalGeneration` capture-default admission with its three arch-scoped gate sites, this row's backend-matrix row + claim + the two checker re-pins (BACKEND count 88→89), and the spec. EXCLUDES: the MoE-family captured arm (`Qwen3_5DecodeGraph`), GGUF k-quant arms (refused by name, owed), the VL tower, Llama/InternLM2 captured pairs (#1627), #1625's async multi-request throughput claim | `ACTIVE` | 2026-09-05 — root cause traced (dual-role shadow pointer replacement at prefill joins); fix landed on branch; captured gate 146/146 (ambient + opt-in), opt-out eager 146/146; both mutations (pair tamper, arch-line delete) red with byte-identical restore; full battery green (backend 52/52, 06b 125/125 both legs, mistral 128/128 both legs, 4B skip unchanged) |
